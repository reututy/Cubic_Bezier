#include "bezier2D.h"


Bezier2D::Bezier2D(void)
{
}

/*
IndexedModel GetSurface(int resT, int resS) 	//generates model for rendering using MeshConstructor::initMeshs
{
	IndexedModel index_model;
	float t = 0.0;
	float s = 0.0;
	float inc_t = (float)(1 / (resT - 1));
	float inc_s = (float)(1 / (resS - 1));
	glm::vec4 vec_t;
	glm::vec4 vec_s;
	glm::vec3 vec_res;

}
*/

Bezier2D::~Bezier2D(void)
{
}
